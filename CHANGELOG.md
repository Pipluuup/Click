# 更新日志（Changelog）

本项目的所有重要变更都会记录在此文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### 修复
- 勾选「自动连发」后改为纯自动模式：手动按键不再叠加触发输出（此前勾选后手动按键仍会额外触发一次）。取消勾选即恢复"按键触发"的手动模式。

## [v1.0.1] - 2026-08-09

### 新增
- 「自动连发」勾选框：勾选后点击「开始」，每 0.05s 自动模拟输出一次 26 个大写字母（随机顺序）。
- 勾选框支持运行中实时勾选 / 取消；手动按键触发保持有效。

### 修复
- 修复 CI 重命名 exe 用 `cp` 导致 Release 出现重复产物的问题（改用 `mv`，仅影响之后发布的版本）。

## [v1.0.0] - 2026-08-09

### 新增
- GUI 界面：开始 / 停止按钮 + 状态显示，全局按 F12 快速停止。
- 全局监听字母键：按下任意字母时吞掉原按键，随机模拟输入 26 个大写字母（含被按下的那个）。
- 按住不连发：系统自动重复只触发一次。
- CI/CD：GitHub Actions 用 PyInstaller 打包 Windows 可执行文件，并自动创建 GitHub Release。
